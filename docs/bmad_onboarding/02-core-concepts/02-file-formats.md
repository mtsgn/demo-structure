---
clickup_workspace_id: "90182277854"
type: "page"
title: "File Formats Explained"
---

# Giải Thích Các File Format Trong BMAD Method

## 🎯 Câu Hỏi 1: Tại Sao Có Nhiều Định Dạng File Khác Nhau?

BMAD Method sử dụng nhiều định dạng file, mỗi loại có mục đích riêng biệt. Đây là thiết kế có chủ đích, không phải ngẫu nhiên.

---

## 📋 Bảng So Sánh Các File Format

| Extension | Mục Đích | Khi Nào Dùng | Ví Dụ File |
|-----------|----------|--------------|------------|
| **`.yaml` / `.yml`** | Configuration & Workflow Definition | Khi cần cấu hình có cấu trúc, dễ đọc bởi cả người và máy | `config.yaml`, `workflow.yaml` |
| **`.md`** (Markdown) | Documentation & Agent Definitions | Khi cần tài liệu dễ đọc hoặc chứa instructions phức tạp | `agent.md`, `workflow.md`, `README.md` |
| **`.csv`** | Tabular Data & Manifests | Khi cần lưu trữ danh sách/bảng dữ liệu có thể query nhanh | `workflow-manifest.csv`, `agent-manifest.csv` |
| **`.xml`** | Structured Data Embedded in Markdown | Khi cần metadata có cấu trúc chặt chẽ trong documentation | XML blocks trong agent `.md` files |

---

## 1️⃣ YAML Files (`.yaml` / `.yml`)

### **Mục đích**: Configuration và Workflow Definition
### **Tại sao dùng YAML?**
- **Human-readable**: Dễ đọc, dễ edit bằng tay
- **Machine-parseable**: Dễ dàng parse bởi code
- **Structured**: Hỗ trợ nested structures, lists, key-value pairs
- **Comments**: Có thể thêm comments với `#`

### **Ví Dụ 1: Configuration File**
File: `_bmad/core/config.yaml`
```yaml
# CORE Module Configuration
# Generated by BMAD installer
# Version: 6.0.0-Beta.2
# Date: 2026-01-28T07:12:00.278Z

user_name: Mazhnguyen
communication_language: Vietnamese
document_output_language: Vietnamese
output_folder: "{project-root}/_bmad-output"
```

**Công dụng**: 
- Lưu trữ user preferences
- AI đọc file này khi khởi động agent để biết tên user, ngôn ngữ, output folder
- Dễ edit: Bạn có thể mở và sửa trực tiếp

### **Ví Dụ 2: Workflow Definition**
File: `_bmad/bmm/workflows/4-implementation/code-review/workflow.yaml`
```yaml
# Workflow định nghĩa các steps, inputs, outputs
name: code-review
description: Adversarial code review workflow
steps:
  - name: analyze-code
    action: review
  - name: find-issues
    action: critique
  - name: propose-fixes
    action: suggest
```

**Công dụng**:
- Định nghĩa workflow một cách có cấu trúc
- AI đọc file này để biết phải làm gì từng bước
- Có thể validate format dễ dàng

---

## 2️⃣ Markdown Files (`.md`)

### **Mục đích**: Documentation và Complex Instructions
### **Tại sao dùng Markdown?**
- **Rich formatting**: Hỗ trợ headers, lists, code blocks, tables
- **Readable as plain text**: Ngay cả khi không render vẫn dễ đọc
- **Universal**: Được hỗ trợ rộng rãi (GitHub, editors, AI IDEs)
- **Can embed other formats**: Có thể chứa code blocks, XML, JSON

### **Ví Dụ 1: Workflow in Markdown**
File: `_bmad/bmm/workflows/2-plan-workflows/create-prd/workflow.md`
```markdown
# PRD Creation Workflow

## Overview
This workflow guides you through creating a comprehensive PRD.

## Steps
1. **Understand the problem**
   - Ask user about the problem they're solving
   - Document pain points
   
2. **Define the solution**
   - Brainstorm solutions
   - Prioritize features
   
3. **Write the PRD**
   - Use template from resources/
   - Fill in all sections
```

**Công dụng**:
- Chứa instructions phức tạp với nhiều formatting
- AI đọc và follow từng bước
- Dễ maintain và đọc bởi humans

### **Ví Dụ 2: Agent Definition**
File: `_bmad/bmm/agents/pm.md`
```markdown
---
name: "pm"
description: "Product Manager"
---

You must fully embody this agent's persona...

```xml
<agent id="pm.agent.yaml" name="John" title="Product Manager" icon="📋">
  <persona>
    <role>Product Manager specializing in collaborative PRD creation</role>
    <identity>Product management veteran with 8+ years</identity>
  </persona>
  <menu>
    <item cmd="CP">Create PRD</item>
    <item cmd="VP">Validate PRD</item>
  </menu>
</agent>
```
```

**Công dụng**:
- Chứa full agent definition
- Front matter (YAML header) cho metadata
- XML embedded để định nghĩa structured agent config
- Markdown cho documentation

**❓ Tại sao embed XML trong Markdown?**
- Markdown cho readability
- XML cho structured agent definition (persona, menu, rules)
- Best of both worlds!

---

## 3️⃣ CSV Files (`.csv`)

### **Mục đích**: Database-like Tables
### **Tại sao dùng CSV?**
- **Fast querying**: Dễ search và filter
- **Tabular data**: Perfect cho lists/catalogs
- **Lightweight**: Không cần database engine
- **Easy to generate**: Code có thể tự động generate

### **Ví Dụ: Workflow Manifest**
File: `_bmad/_config/workflow-manifest.csv`
```csv
name,description,module,path
"brainstorming","Facilitate interactive brainstorming sessions","core","_bmad/core/workflows/brainstorming/workflow.md"
"create-prd","PRD tri-modal workflow","bmm","_bmad/bmm/workflows/2-plan-workflows/create-prd/workflow.md"
"create-architecture","Collaborative architectural decisions","bmm","_bmad/bmm/workflows/3-solutioning/create-architecture/workflow.md"
```

**Công dụng**:
- **Central catalog**: BMAD biết workflows nào có sẵn
- **Fast lookup**: Khi user gọi `/create-prd`, BMAD search CSV này
- **Easy to aggregate**: Combine data từ nhiều modules

**Quy trình hoạt động**:
```
User gọi: /create-prd
    ↓
AI search trong: workflow-manifest.csv
    ↓
Tìm thấy row: name="create-prd", path="_bmad/bmm/workflows/2-plan-workflows/create-prd/workflow.md"
    ↓
AI load file tại path đó
    ↓
Execute workflow
```

### **Ví Dụ 2: Agent Manifest**
File: `_bmad/_config/agent-manifest.csv`
```csv
name,title,icon,module,path
"pm","Product Manager","📋","bmm","_bmad/bmm/agents/pm.md"
"architect","Software Architect","🏗️","bmm","_bmad/bmm/agents/architect.md"
"dev","Developer","💻","bmm","_bmad/bmm/agents/dev.md"
```

**Công dụng**: Tương tự workflow manifest, nhưng cho agents

---

## 4️⃣ XML Embedded in Markdown

### **Tại sao dùng?**
BMAD không dùng standalone XML files, nhưng **embed XML trong Markdown** để:
- **Structured agent config**: XML cho strict schema
- **Human-readable wrapper**: Markdown cho documentation context

### **Ví Dụ: Agent Definition**
```markdown
---
name: "pm"
---

```xml
<agent id="pm.agent.yaml">
  <activation critical="MANDATORY">
    <step n="1">Load persona</step>
    <step n="2">Load config.yaml</step>
    <step n="3">Show menu</step>
  </activation>
  
  <persona>
    <role>Product Manager</role>
    <communication_style>Direct and data-sharp</communication_style>
  </persona>
  
  <menu>
    <item cmd="CP" exec="{project-root}/_bmad/bmm/workflows/2-plan-workflows/create-prd/workflow.md">
      [CP] Create PRD
    </item>
  </menu>
</agent>
```
```

**Tại sao XML thay vì YAML cho agent?**
- **Strict validation**: XML có schema validation chặt chẽ hơn
- **Attributes**: XML có attributes (`cmd="CP"`, `n="1"`) tiện cho config
- **Nesting**: XML rất tốt cho deeply nested structures

---

## 🤔 Câu Hỏi 2: Tại Sao Core Chỉ Có 1 Agent (BMad Master)?

Đây là thiết kế có chủ đích! Hãy so sánh:

### **Core Module**
```
_bmad/core/agents/
└── bmad-master.md   # Only 1 agent
```

**Lý do**:
1. **Universal orchestrator**: `bmad-master` là "master conductor"
2. **Not domain-specific**: Core không liên quan đến PM, Dev, UX, etc.
3. **Meta-agent**: Nhiệm vụ là điều phối, không phải thực thi domain work

**Chức năng của bmad-master**:
- Load và execute workflows từ bất kỳ module nào
- Quản lý resource catalog (list tasks, workflows, agents)
- Party mode orchestration (gọi nhiều agents khác)
- Help system routing

### **BMM Module (Agile Development)**
```
_bmad/bmm/agents/
├── analyst.md           # 📊 Business Analyst
├── architect.md         # 🏗️ Software Architect
├── dev.md               # 💻 Developer
├── pm.md                # 📋 Product Manager
├── sm.md                # 🎯 Scrum Master
├── tea.md               # ✅ Test Engineer
├── tech-writer/         # 📝 Technical Writer
├── ux-designer.md       # 🎨 UX Designer
└── quick-flow-solo-dev.md
```

**Lý do có nhiều agents**:
1. **Domain expertise**: Mỗi agent chuyên về một role cụ thể
2. **Persona-based**: PM agent có tính cách khác Dev agent
3. **Menu specialization**: Mỗi agent có menu workflows phù hợp với role

### **So Sánh Cụ Thể**

#### BMad Master Agent (Core)
```xml
<agent name="BMad Master" title="Workflow Orchestrator" icon="🧙">
  <role>Master Task Executor + BMad Expert</role>
  <menu>
    <item>[LT] List Available Tasks</item>
    <item>[LW] List Workflows</item>
    <item>[PM] Start Party Mode</item>
  </menu>
</agent>
```
→ **Generic orchestrator**, không có domain-specific workflows

#### PM Agent (BMM Module)
```xml
<agent name="John" title="Product Manager" icon="📋">
  <role>Product Manager specializing in PRD creation</role>
  <communication_style>Asks 'WHY?' relentlessly</communication_style>
  <menu>
    <item>[CP] Create PRD</item>
    <item>[VP] Validate PRD</item>
    <item>[CE] Create Epics and Stories</item>
  </menu>
</agent>
```
→ **Domain expert**, có personality và specialized workflows

---

## 📊 Kiến Trúc Design Pattern

```
┌─────────────────────────────────────────────────┐
│           CORE MODULE (Universal)               │
│  ┌──────────────────────────────────────┐      │
│  │  BMad Master Agent (Orchestrator)     │      │
│  │  - Load workflows from any module     │      │
│  │  - Manage resources                   │      │
│  │  - Party mode coordination            │      │
│  └──────────────────────────────────────┘      │
└─────────────────────────────────────────────────┘
                      │
                      ├─── Routes to ───┐
                      │                   │
┌─────────────────────────────┐  ┌──────────────────────┐
│   BMM Module (Agile)        │  │  Other Modules...    │
│  ┌───────────────────┐      │  │                      │
│  │ 9 Specialized     │      │  │  ┌─────────────┐    │
│  │ Domain Agents:    │      │  │  │ Game Dev    │    │
│  │ - PM              │      │  │  │ Agents      │    │
│  │ - Architect       │      │  │  └─────────────┘    │
│  │ - Developer       │      │  │                      │
│  │ - UX Designer     │      │  │  ┌─────────────┐    │
│  │ - etc.            │      │  │  │ Creative    │    │
│  └───────────────────┘      │  │  │ Agents      │    │
└─────────────────────────────┘  │  └─────────────┘    │
                                  └──────────────────────┘
```

---

## 💡 Best Practices

### 1. **Khi Nào Dùng File Format Gì?**

| Scenario | Recommended Format | Lý Do |
|----------|-------------------|-------|
| User configuration | `.yaml` | Dễ edit, human-readable |
| Agent definition | `.md` with embedded XML | Rich documentation + structured config |
| Workflow with many steps | `.md` | Formatting flexibility |
| Simple workflow | `.yaml` | Clean structure |
| Catalog/Manifest | `.csv` | Fast lookup, tabular data |
| Documentation | `.md` | Rich formatting |

### 2. **Customization**

**File có thể edit trực tiếp**:
- ✅ `_bmad/core/config.yaml` - User preferences
- ✅ `_bmad/_config/agents/*.customize.yaml` - Agent customization
- ❌ `_bmad/_config/*.csv` - Auto-generated manifests (đừng edit)
- ❌ `_bmad/bmm/workflows/**/*.yaml` - Core workflows (use BMad Builder để customize)

---

## 🔍 Ví Dụ Thực Tế: File Format Flow

### Scenario: User gọi `/pm` (load PM agent)

**Bước 1**: AI search trong `_bmad/_config/agent-manifest.csv`
```csv
name,module,path
"pm","bmm","_bmad/bmm/agents/pm.md"
```

**Bước 2**: AI load file `_bmad/bmm/agents/pm.md`
```markdown
---
name: "pm"
description: "Product Manager"
---

```xml
<agent id="pm.agent.yaml">
  <activation>
    <step n="2">Load config.yaml</step>
  </activation>
</agent>
```
```

**Bước 3**: AI load `_bmad/bmm/config.yaml` (theo XML instruction)
```yaml
user_name: Mazhnguyen
communication_language: Vietnamese
```

**Bước 4**: AI load customization `_bmad/_config/agents/bmm-pm.customize.yaml`
```yaml
# User's custom settings cho PM agent
memories:
  - "User prefers detailed planning"
```

**Bước 5**: AI activate PM agent với:
- Persona từ `pm.md` (XML block)
- Config từ `config.yaml`
- Customization từ `customize.yaml`

---

## 📚 Tóm Tắt

### **File Formats**:
1. **YAML** → Configuration, workflow definitions (structured, editable)
2. **Markdown** → Documentation, complex workflows, agent definitions (readable, rich formatting)
3. **CSV** → Manifests, catalogs (fast lookup, tabular)
4. **XML** → Embedded trong Markdown cho structured agent config

### **Core vs BMM Agents**:
- **Core**: 1 agent (`bmad-master`) - Universal orchestrator
- **BMM**: 9 agents - Domain experts với specialized personas

### **Thiết Kế Principle**:
- **Separation of concerns**: Core = orchestration, Modules = domain work
- **Right tool for the job**: Mỗi format phục vụ mục đích riêng
- **Human + Machine**: Files phải dễ đọc bởi cả người và AI
